# PickNRoll — instrucciones para Claude

Simulador de carrera de básquet por decisiones, estilo "Copero" (career sim de fútbol) pero NBA. Foco en mecánica de progresión difícil de dominar (potencial oculto + pool de opciones filtrado por estado actual, no libre elección).

## Al iniciar cada sesión

1. Leer `MVP_SPEC.md` (documento maestro — arquitectura, stack, mecánica, roadmap).
2. Leer la sección **Estado** de este archivo.
3. Continuar con la fase que corresponda. No saltear fases.

## Reglas clave

- Stack: React + Vite + TS + Tailwind + Zustand. Sin backend en el MVP (localStorage). No reabrir sin motivo concreto.
- `src/engine/` = lógica pura del juego, sin React. Tests con Vitest ANTES de tocar la UI (misma regla que PixGeo/Control de Gastos).
- El motor no sabe nada de estética — la estética se define aparte con **Claude Design** (herramienta externa) y se integra después importando los archivos generados, mismo patrón que el rediseño ámbar de PixGeo (pisar/mergear componentes con cuidado de no romper wiring existente).
- **Nombres y escudos de equipos NBA reales** — decisión explícita del usuario pese al riesgo de marca/copyright ya señalado (ver MVP_SPEC §5). No reabrir esta discusión salvo que el usuario lo pida.
- Mecánica de dificultad es la pieza central del diseño: potencial oculto con distribución sesgada + pool de eventos filtrado por OVR actual (no por potencial) → efecto bola de nieve intencional. No simplificar esto sin confirmar con el usuario.
- No agregar features fuera de lo acordado en `MVP_SPEC.md` sin confirmar primero (mismo criterio que PixGeo/Liberkuka).

## Estado

- **2026-07-27**: proyecto creado. `MVP_SPEC.md` y este archivo definidos. Decisiones cerradas: stack (igual PixGeo), mecánica de progresión (RNG ponderado + potencial oculto sesgado + pool de eventos filtrado por OVR), equipos reales (NBA, con riesgo asumido), retiro a los 40. **Fase 0 completa**: scaffold Vite+React+TS, Tailwind v4, Vitest+jsdom+Testing Library, Zustand, oxlint. `.claude/launch.json` puerto 5198. `tsc -b`, `npm run build` y `oxlint` limpios. Git inicializado, sin commits todavía.
- **2026-07-27 (cont.)**: sección "Resultados NBA" (idea del usuario: sitio con pestaña de juego + resultados live/pasados/futuros estilo Promiedos). Se investigaron fuentes gratis a fondo con tests reales (no solo research teórico): `nba.com` (CDN/stats) descartado — bloquea 403 IPs de datacenter/cloud (Akamai), inviable en producción. BALLDONTLIE descartado — free real es 48hs. Big Balls Sports Data descartado — sin reputación verificable. **API-Sports NBA v2** (cuenta ya creada por el usuario) probado con key real: free tier da historial completo de temporadas cerradas (2022-25, 1387 partidos confirmados) pero bloquea 100% la temporada en curso/hoy. **ESPN no-oficial** (`site.api.espn.com/apis/site/v2/sports/basketball/nba/scoreboard`) resultó la fuente ganadora para hoy/en vivo/calendario — sin key, sin cuota, sin bloqueo, verificado con datos reales. Arquitectura: `api/nba/scoreboard.ts` (proxy a ESPN, cache adaptativo 30s si hay partido en vivo / 15min si no) y `api/nba/games.ts` (proxy a API-Sports, respaldo opcional no bloqueante). Ninguno probado en el preview de Vite todavía (necesita `vercel dev`, no configurado) — sí verificados por curl con datos reales y por tsc/lint. Nota de seguridad: la API key de API-Sports circuló en texto plano en el chat; se recomendó resetear, el usuario decidió explícitamente no hacerlo y usarla igual (riesgo asumido, no omisión).
- **2026-07-27 (cont. 2)**: **Fase 1 — Motor de progresión completa**, con tests ANTES de la UI (como pide la regla del proyecto). `src/engine/potencial.ts` (potencial oculto sesgado: 70% techo 60-75, 20% 76-85, 8% 86-92, 2% 93-99). `src/engine/progresion.ts` (`avanzarTemporada`: curva por edad — crecimiento 19-23, maduración 24-27, meseta 28-31, declive 32+ — clampeada entre piso 30 y el potencial oculto, nunca lo supera). `src/engine/eventos.ts` (`elegirEquiposOfrecidos`: pool de hasta 3 equipos filtrado por `nivel <= ovrActual + 10` — la pieza central de dificultad, efecto bola de nieve).
- **2026-07-27 (cont. 3)**: dataset real `src/engine/datos/equiposNba.ts` (30 franquicias NBA reales, campo `nivel` de balance aproximado, candidato a alimentarse con standings reales de `api/nba/scoreboard.ts` más adelante). **Camino pre-NBA agregado** (idea del usuario, confirmada explícitamente): la carrera ya no arranca en la NBA — primer evento es "elegir camino" (universidad NCAA / G-League / liga internacional, `src/engine/caminosPreNba.ts`). El Draft NBA no es automático: se dispara solo si el OVR cruza `UMBRAL_DRAFT_OVR` (55) mientras estás en tu camino; si nunca lo cruzás antes del retiro a los 40, la carrera entera transcurre sin pisar la NBA — decisión de dificultad confirmada por el usuario, con test dedicado. `src/engine/motorCarrera.ts` reescrito: `Carrera` ahora tiene `fase: 'pre-nba'|'nba'` y `caminoPreNba`; `elegirEquipo` renombrado a `elegirOpcion` (unifica resolución de los 3 tipos de evento: `elegir-camino`/`draft`/`trade`).
- **2026-07-27 (cont. 3.5)**: **Sistema de diseño recibido de Claude Design** (`PickNRoll sistema de diseño.zip`, formato `.dc.html` propio de esa herramienta — no es código React ejecutable, es un mockup interactivo con custom elements que no corre en nuestro navegador por CSP; se leyó el HTML crudo para extraer tokens/estructura). Dirección: **"pizarra táctica + parquet + scoreboard de estadio"** — fondo casi negro (`#0d0d0d`), superficie (`#1a1512`/`#241d17`), acento naranja pelota (`#ff6b1a`), hueso/línea de cancha (`#f5f1e8`), tipografía Bebas Neue (marcador/OVR/números grandes) + Oswald (títulos/equipos) + Roboto Mono (planillas/tablas), sombra sólida offset 6px sin blur, texturas de parquet/líneas de cancha/grano sutil. Cubre 7 pantallas: tokens, Resultados·día, Resultados·detalle, Setup jugador, Evento de decisión, Progreso/historial, Retiro — más variantes mobile. Antes de construir la UI se detectó que el diseño esperaba PPG/RPG/APG/trofeos que el motor no calculaba todavía — el usuario confirmó extender el motor primero.
- **2026-07-27 (cont. 3.6)**: `src/engine/estadisticas.ts` (`calcularEstadisticasTemporada`: PJ/PPG/RPG/APG derivados de OVR + rol — titular/rotación/banca según diferencia con el nivel del club; 82 PJ en NBA, 34 fuera — valor fijo aproximado, no calendario real). `src/engine/trofeos.ts` (`evaluarTrofeosTemporada`: All-Star determinista con OVR≥80, MVP con OVR≥90 + algo de azar, anillo con club nivel≥85 + azar — solo en fase NBA, liga doméstica/universidad no otorga trofeos en el MVP). `motorCarrera.ts`: `EntradaHistorial` ahora incluye las estadísticas de cada temporada, `Carrera.trofeos` acumula durante toda la carrera. 46 tests Vitest pasando, `tsc -b` y `oxlint` limpios (se descartó la carpeta de extracción temporal del zip de diseño tras leer los tokens, para no ensuciar el lint).
- **2026-07-27 (cont. 4)**: **nacionalidad ahora determina el camino pre-NBA real** (idea del usuario: si sos argentino arrancás en Boca/Ferro/etc., si sos español en Real Madrid/Barcelona/etc. de básquet, no en universidad genérica). `src/engine/datos/ligasPorPais.ts` (nuevo): 10 países con liga doméstica curada (Argentina LNB, España ACB, Italia, Grecia, Serbia, Turquía, Francia, Australia NBL, Lituania LKL, Brasil NBB), con comentario "CÓMO ACTUALIZAR" pensado para revisión periódica (pedido explícito del usuario: "algo muy profesional para que cada tanto podamos revisar y actualizar"). Países sin liga curada (incluye EEUU a propósito) caen al camino genérico ya existente — decisión confirmada por el usuario. `motorCarrera.ts`: `crearCarrera` ahora recibe `codigoPaisNacionalidad` (ISO alpha-2), nuevo tipo de evento `club-liga-domestica` (mismo mecanismo de filtro por OVR que draft/trade), `Carrera.nacionalidad` guardada.
- **2026-07-27 (cont. 4.5)**: **Fase 2 — UI del juego construida** sobre el sistema de diseño de Claude Design (tokens ya integrados en `src/index.css` vía `@theme` de Tailwind v4: colores, fuentes Bebas Neue/Oswald/Roboto Mono, utilidades `.sombra-brutal`/`.linea-jugada`/`.textura-parquet`/`.textura-grano`). 4 pantallas en `src/ui/`: `PantallaSetup.tsx` (nombre/apellido/dorsal/posición/mano — cosmético, no llega al motor —, nacionalidad, camino pre-NBA si aplica, dificultad), `PantallaEvento.tsx` (HUD + cards de opciones, distingue `elegir-camino` de los eventos con equipos reales), `PantallaProgreso.tsx` (OVR grande + curva simple, historial con PJ/PPG/RPG/APG reales, vitrina de trofeos, botón seguir jugando), `PantallaRetiro.tsx` (resumen + texto para compartir con Web Share/clipboard, mismo patrón que PixGeo). `App.tsx` orquesta las 4 pantallas contra `motorCarrera.ts` con `Math.random` (sin semilla — carrera libre, no desafío diario todavía).
  - Bug propio encontrado y corregido en la verificación manual: el historial mostraba el ID crudo del club (`AR-QUIMSA`) para temporadas pasadas en vez del nombre — `EntradaHistorial` solo guardaba `clubId`. Se agregó `clubNombre` guardado directo en el momento de cada temporada (`motorCarrera.ts`).
  - Verificado a mano jugando una carrera real: setup → Draft/evento con clubes reales de Argentina filtrados por OVR → progreso con historial y stats coherentes (OVR subiendo de 42 a 58 en 2 temporadas, cruce de umbral, salto directo a Draft NBA con Charlotte Hornets/Sacramento Kings/San Antonio Spurs). Sin errores de consola. `getComputedStyle` confirmó tokens aplicados (Oswald, naranja `#ff6b1a`, sombra sólida offset). 46 tests Vitest, `tsc -b` y `oxlint` limpios.
  - Verificado de punta a punta con un script que clickea automáticamente hasta el retiro: carrera completa de 21 temporadas, pico OVR 71, llegó a la NBA, PJ total 1626, texto de compartir armado correctamente, sin errores de consola en ningún punto. "COPIAR" no cambia a "COPIADO ✓" en este navegador de test porque el Clipboard API está bloqueado sin gesto de usuario real — es el fallback ya programado (no crashea), comportamiento esperado, no un bug. "NUEVA CARRERA" resetea limpio a la pantalla de setup.
  - Pendiente: pantalla de Resultados NBA (backend `api/nba/scoreboard.ts` ya listo, falta el front). Pendiente de diseño (anotado por el usuario, no implementado): eventos aleatorios estilo Copero que suban/bajen el OVR entre decisiones, más allá de la curva de edad y el gate de equipos ya existentes (ver MVP_SPEC.md §6.1).
- **2026-07-27 (cont. 6)**: **setup rehecho como wizard de 4 pasos** (pedido explícito del usuario, reemplaza la pantalla única anterior):
  1. `PasoCaminoDificultad.tsx` — camino pre-NBA (liga doméstica/vía EEUU, sin nombrar la liga específica todavía porque el país se elige después) + velocidad de decisiones. Botón Continuar.
  2. `PasoPais.tsx` — selector estilo Copero: buscador + lista completa de países (bandera real vía flagcdn + nombre en español, `i18n-iso-countries` — mismo paquete que ya funcionó en PixGeo) con selección y checkmark. Continuar/Volver.
  3. `PasoCamiseta.tsx` — camiseta SVG coloreada según el país elegido (`datosColoresPais.ts`, paleta curada para los países con liga + EEUU, resto cae a un color neutro del propio sistema de diseño, no inventado al azar) con APELLIDO y DORSAL actualizándose en vivo sobre el dibujo mientras se tipea, + mano hábil. Continuar/Volver.
  4. `PasoPosicion.tsx` — cancha estilizada (textura de parquet con tablones + brillo diagonal vía gradientes CSS, líneas de cancha en SVG) con las 5 posiciones como puntos clickeables ubicados en su zona real (Base arriba, Escolta/Alero en las alas, Ala-Pívot/Pívot cerca del aro). Continuar ("EMPEZAR CARRERA")/Volver.
  - `PantallaSetupWizard.tsx` orquesta los 4 pasos y arma el objeto final para `crearCarrera` al terminar. Tipo `DatosSetup` movido a `tiposSetup.ts` (antes vivía en el archivo de pantalla único, ya borrado). Se sacó el campo "nombre" del setup — ahora solo se pide apellido (la camiseta real de básquet no lleva nombre de pila).
  - Verificado a mano jugando el flujo completo: paso 1→2 (Argentina con bandera real vía buscador)→3 (camiseta celeste/blanca con "FERRARI"/"14" actualizando en vivo)→4 (cancha con las 5 posiciones visibles, Base seleccionada)→arranca el juego mostrando clubes reales de Argentina (Ferro/Quimsa/Obras) filtrados por OVR. Sin errores de consola nuevos (los que aparecieron eran residuales del HMR durante el borrado del archivo viejo). tsc/lint limpios, 46 tests sin tocar (el cambio es 100% de UI, no de motor).
- **2026-07-27 (cont. 7)**: el usuario reportó la carrera como "0 divertido, sin ningún fin" — se implementó lo que ya estaba anotado como pendiente más un cierre real:
  - `src/engine/sucesos.ts` (`generarSuceso`): tercera fuente de variación por temporada — lesión (~12%, -3 a -6 OVR), racha (~10%, +2 a +4), conflicto con el técnico (~8%, -1 a -3), resto sin sobresaltos. `progresion.ts` ganó `clampOvr`/`OVR_MINIMO` exportados (se usa tanto en `avanzarTemporada` como al aplicar el delta del suceso en `motorCarrera.ts`, para no duplicar el clamp).
  - `src/engine/veredicto.ts` (`calcularVeredicto`): título+descripción final según pico histórico de OVR y trofeos — NUNCA LLEGÓ A LA NBA / LEYENDA / ESTRELLA NBA / BUEN TITULAR / JUGADOR DE ROTACIÓN.
  - `EntradaHistorial` ganó `sucesoTexto`/`sucesoDeltaOvr`. `PantallaProgreso.tsx` muestra un banner (verde/rojo) con el suceso de la última temporada cuando hubo alguno. `PantallaRetiro.tsx` muestra el veredicto como cartel destacado y lo suma al texto para compartir.
  - Verificado jugando una carrera completa hasta el retiro con un script automatizado: veredicto "JUGADOR DE ROTACIÓN" con pico OVR 66 mostrado correctamente en pantalla y en el texto de compartir. 58 tests Vitest pasando, `tsc -b` y `oxlint` limpios.
- **2026-07-27 (cont. 8)**: el usuario pidió específicamente jugar Copero a fondo para calcar sus 3 modos y mecánica real, en vez de inventar. Hallazgos jugando `copero.com.ar/juegos/simulador-carrera`: Exprés real = cada 3 temporadas (no 4, corregido). Copero tiene dos familias de evento — club (cantera/mercado de pases) y **decisión de riesgo** (ej. "Doble turno": 65% Titular/35% Lesión vs. seguro; "Plan de alimentación": 60% +3 OVR/40% -2 OVR vs. sin cambios) — la probabilidad se muestra ANTES de elegir. Se reemplazó el sistema anterior de "sucesos" automáticos e invisibles por este patrón real: `src/engine/decisionesRiesgo.ts` (`generarDecisionRiesgo`, 3 temáticas de básquet, resultado ya resuelto al generar el evento), nuevo tipo de evento `riesgo` en `motorCarrera.ts` (pre-nba: siempre riesgo mientras no se cruza el umbral; NBA: 50/50 entre trade y riesgo), `Carrera.ultimoResultadoRiesgo` para mostrar el resultado. `PantallaEvento.tsx` tiene layout dedicado para `riesgo` (arriesgar con ambos desenlaces y %, o jugar seguro). `PantallaProgreso.tsx` muestra el resultado como banner verde/rojo. Verificado jugando en vivo: apareció "Doble turno" 65%/35%, se arriesgó, salió mal, OVR bajó -4 y el banner lo mostró correctamente. 60 tests Vitest pasando, `tsc -b` y `oxlint` limpios.
- **2026-07-27 (cont. 9)**: feedback duro del usuario tras probarlo — "esta muy apagada la animación... bastante feo... el banner de arriba queda horrible... siempre pregunta doble turno". Dos problemas reales, uno de diseño y uno de mecánica:
  - **Mecánica**: en fase pre-NBA el motor generaba SIEMPRE una decisión de riesgo (nunca cambio de club) — diseño mío, no bug de azar (el tema sí rotaba entre las 3 opciones, pero la repetición de "Doble turno" en la sesión del usuario fue mala suerte real con solo 3 temas + la monotonía de nunca variar de TIPO de evento). Se corrigió: `avanzarSiCorresponde` ahora acepta un cuarto parámetro opcional `equiposDomesticos: Equipo[]` — si la nacionalidad tiene liga curada, la fase pre-NBA también alterna 50/50 entre pase de club y decisión de riesgo (antes solo alternaba así en NBA). `App.tsx` pasa `LIGAS_POR_PAIS[carrera.nacionalidad]?.clubes ?? []`. Sin liga curada (camino genérico), sigue siendo 100% riesgo (no hay pool de clubes que ofrecer).
  - **Diseño**: se sacó el banner plano de ancho completo arriba de todo (franja de color lavado, sin borde, se sentía "pegado" y no del sistema de diseño). Ahora el resultado de la última decisión de riesgo es un **chip** con borde+sombra sólida (`.sombra-brutal`), integrado justo debajo del número de OVR en `PantallaProgreso.tsx` — coherente con el resto de la estética. Se agregó animación real (`@keyframes ovr-pop` en `index.css`): el número de OVR "pega un salto" con un pequeño overshoot al entrar a la pantalla (usa `key={carrera.historial.length}` para que React remonte el elemento y la animación se dispare de nuevo cada vez), y el chip tiene su propia animación de entrada (`chip-entrada`).
  - 61 tests Vitest pasando (test nuevo: pre-NBA con equipos disponibles alterna a pase de club), `tsc -b` y `oxlint` limpios. Verificado jugando una carrera completa en el navegador: variedad real de eventos en pre-NBA (Ferro→Boca→Draft), chip de riesgo con el nuevo estilo integrado, sin franja fea. Nota de entorno: la consola del navegador de test acumula errores viejos (del HMR de un archivo ya borrado hace rato) que persisten entre navegaciones/reinicios del server — confirmado que es un artefacto de la herramienta, no un error real, abriendo una pestaña nueva (0 errores ahí).
- **2026-07-27 (cont. 10)**: **escudos reales de la NBA** (pedido del usuario). Confirmado que ESPN (misma fuente que ya usamos para resultados) sirve escudos reales sin key en `a.espncdn.com/i/teamlogos/nba/500/{slug}.png` — se sacó la lista oficial de 30 abreviaturas/slugs desde `site.api.espn.com/.../nba/teams` para mapear bien los casos donde nuestro id interno no coincide con el slug de ESPN (nyk→ny, gsw→gs, nop→no, sas→sa, uta→utah, was→wsh). `Equipo` (`eventos.ts`) ganó campo opcional `escudoUrl`; `equiposNba.ts` con las 30 URLs reales; `PantallaEvento.tsx` (cards de evento) y `PantallaProgreso.tsx` (sección EQUIPO) muestran el escudo real si existe, si no caen al badge de iniciales de siempre (fallback ya construido, sin romper nada para ligas domésticas que todavía no tienen escudo).
  - **Ligas domésticas (Argentina/España/etc., ~55 clubes) quedan pendientes** — decisión explícita del usuario: no hay un API único como ESPN para esas ligas, hay que buscar cada logo a mano (Wikipedia/Wikimedia u otra fuente), tarea más grande que se hace en otra pasada. Por ahora siguen con el badge de iniciales.
  - Verificado jugando en el navegador hasta el Draft NBA: escudos reales de Memphis Grizzlies/Charlotte Hornets/Detroit Pistons cargando correctamente en las cards. 61 tests, `tsc -b` y `oxlint` limpios (sin tests nuevos — son URLs de imagen, no lógica).
- **2026-07-27 (cont. 11)**: dos pedidos del usuario en un mismo mensaje — (1) integrar animaciones de un nuevo paquete de Claude Design sin cambiar las pantallas existentes; (2) darle más "onda" a puntos/rebotes/asistencias/triples y agregar una decisión aleatoria de especialización (triplero u otra opción) a mitad de carrera cuando la posición lo amerita.
  - Animaciones (`index.css`, extraídas del prototipo `.dc.html`): tiro al aro (`ball-flight`/`net-wobble`/`rim-flash`/`swish-pop`), pulso de glow en botones CTA, entrada de trofeos (`trophy-in`), asentamiento de texto (`settle-bounce`), pop de delta numérico (`delta-pop`). `AnimacionAro.tsx` (nuevo) — animación de "fichaje" (tablero+aro+pelota+"¡FICHADO!") que se dispara al elegir club en `PantallaEvento.tsx`, antes de confirmar la elección real contra el motor. Trofeos de `PantallaProgreso.tsx`/`PantallaRetiro.tsx` y botones CTA de todo el wizard ganaron las animaciones nuevas.
  - **Especialización** (`src/engine/especializacion.ts`, nuevo): a partir de los 21 años y solo en posiciones de perímetro (PG/SG/SF), con ~18% de probabilidad por temporada, aparece una decisión única e irreversible: "Triplero" (más triples, algo menos rebotes) o "Anotador interior" (más rebotes, casi sin triples) — multiplicadores aplicados en `estadisticas.ts`. `motorCarrera.ts`: `crearCarrera` ahora recibe `posicion` (antes se pedía en el wizard pero no llegaba al motor — se conectó de una), `Carrera.especializacion`, nuevo tipo de evento `especializacion` resuelto en `elegirOpcion`. `PantallaProgreso.tsx` muestra la especialización elegida como badge.
  - Estadísticas con "más onda": `EstadisticasTemporada` ganó `triples`; historial y "última temporada" en `PantallaProgreso.tsx` muestran 3PM como columna/stat propia.
  - 94 tests Vitest pasando (10 nuevos de especialización + ajustes en fixtures existentes), `tsc -b` y `oxlint` limpios. Verificado por tests que la posición C nunca dispara la decisión y que perímetro sí; la verificación visual en navegador de la pantalla de especialización quedó pendiente en esta sesión (se interrumpió por el pedido siguiente) — no bloqueante, cubierto por tests.
- **2026-07-27 (cont. 12)**: pedido del usuario: "agregar equipos y todos los escudos" a las ligas domésticas (pendiente marcado en la entrada anterior) + mostrar esos escudos también en la lista de historial.
  - **Escudos reales para los ~51 clubes existentes de `ligasPorPais.ts`**: resueltos vía API pública de Wikipedia (búsqueda + imagen de infobox, sin key), mismo patrón ya usado para investigar fuentes en la entrada de "Resultados NBA". 48/51 resueltos; quedan sin escudo (fallback a badge de iniciales, ya soportado) `fr-asvel` (el resultado de búsqueda matcheaba una foto del estadio, no el logo), `fr-strasbourg` (matcheaba un jugador no relacionado) y `au-nz-breakers` (matcheaba un artículo de temporada, no el club).
  - **16 clubes nuevos agregados** (uno o dos por liga, para que "agregar equipos" fuera real y no solo cosmético): Argentina (Gimnasia La Plata, Estudiantes Bahía Blanca), España (Gran Canaria, Manresa), Italia (Fortitudo Bologna, Trieste), Grecia (Promitheas Patras), Serbia (Vojvodina), Turquía (Bahçeşehir Koleji), Francia (Nanterre 92, Cholet), Australia (Adelaide 36ers, Illawarra Hawks), Lituania (Lietkabelis), Brasil (Bauru Basket, Corinthians). 15/16 con escudo resuelto — `br-corinthians` sin imagen usable en su página de Wikipedia, queda con badge de iniciales. `nivel` asignado a mano para cada uno, ubicándolo entre los clubes ya existentes de su liga según su fuerza real aproximada.
  - **Escudos en el historial**: `EntradaHistorial` (`motorCarrera.ts`) ganó `clubEscudoUrl`, snapshoteado igual que `clubId`/`clubNombre` en cada temporada jugada. `PantallaProgreso.tsx` muestra el escudo (16×16, fallback silencioso si no hay) al lado del nombre del club en cada fila del historial, no solo en las cards de elección de evento.
  - **Nota de proceso**: la primera pasada de scripts de resolución (Python, fuera del repo) tiró dos problemas ajenos al resultado final — un crash de encoding de consola de Windows con caracteres turcos/serbios/lituanos (se solucionó escribiendo a JSON en vez de imprimir) y falsos 429 "rate limit" de Wikimedia al verificar en bulk con requests scripteados seguidos (confirmado como artefacto propio, no un problema real, chequeando a mano en el navegador real que las URLs cargan bien).
  - Verificado a mano en el navegador: wizard completo eligiendo Argentina → cards de evento con escudos reales de San Lorenzo/Obras Sanitarias/Ferro Carril Oeste → tras elegir Obras Sanitarias y avanzar una temporada, el historial muestra su escudo junto al nombre. 86 tests Vitest pasando, `tsc -b` y `oxlint` limpios.
- **2026-07-27 (cont. 11)**: **animación de crecimiento + color por nivel** (pedido del usuario: ver crecer la media y que suba de color hasta dorado en nivel altísimo). `src/ui/colorOvr.ts` (`colorPorOvr`): interpola suavemente entre 5 paradas de color — gris apagado (30) → hueso (50) → naranja acento (65) → naranja-dorado (82) → dorado brillante (93+, con `esNivelElite` a partir de 90 para el brillo/`text-shadow` dorado). `src/ui/useNumeroAnimado.ts`: hook que cuenta de forma animada (ease-out, 700ms, `requestAnimationFrame`) desde el valor anterior al nuevo, en vez de saltar de golpe. Aplicado en `PantallaProgreso.tsx` (OVR grande + columna OVR del historial) y `PantallaEvento.tsx` (OVR del HUD).
  - Verificado jugando en el navegador: el color pasa de gris/hueso (OVR 44-52) a naranja acento (56-63) de forma visible en el historial, escalando correctamente. Sin errores de consola. 61 tests, `tsc -b` y `oxlint` limpios (sin tests nuevos — son puramente de presentación/animación, no lógica de negocio).
  - Pendiente de verificar en una sesión futura: el tramo dorado (OVR 90+) y el brillo/`text-shadow` — no se alcanzó ese nivel en la corrida de prueba (haría falta una carrera más larga o forzar el azar).
- **2026-07-27 (cont. 12)**: feedback muy duro del usuario — "recontra aburrido, poco azar, poca probabilidad de crecer, no hay equipo universitario para elegir, no veo animación buena, todo horrible". Tres problemas reales encontrados y corregidos, no solo cosmética:
  - **Bug real de la animación**: `PantallaProgreso` se desmonta y remonta cada vez que `App.tsx` cambia de pantalla (render condicional) — el estado interno de `useNumeroAnimado` se perdía en cada montaje, así que la animación de conteo NUNCA se disparaba en la práctica (arrancaba siempre igual al valor objetivo, sin nada que animar). Se corrigió pasando el valor anterior explícito desde `carrera.historial` (`anterior?.ovr`) como segundo argumento del hook, en vez de que el hook dependa de memoria interna que no sobrevive el remontaje.
  - **Carencia real: "vía EEUU" no ofrecía ningún equipo** — antes era un evento `elegir-camino` con 3 etiquetas sin club real (universidad/G-League/internacional como texto plano, sin clubActual asignado nunca). Se creó `src/engine/datos/equiposCaminoGenerico.ts` (Duke, Kentucky, Kansas, North Carolina, UCLA, Gonzaga, G League Ignite, Villanova + 2 clubes europeos genéricos — mismo criterio de riesgo ya asumido para las ligas profesionales). El tipo de evento `elegir-camino` se eliminó por completo del motor (`motorCarrera.ts`, `caminosPreNba.ts`, `PantallaEvento.tsx`) — ahora el camino genérico usa el mismo mecanismo de evento de club que las ligas domésticas (`club-liga-domestica`), con clubActual real asignado desde el primer evento. `Carrera` ganó `poolPreNba: Equipo[]` (guardado una vez al crear la carrera) reemplazando el viejo `caminoPreNba` (que era solo metadata sin efecto real).
  - **Muy poco azar/crecimiento**: se ampliaron los rangos de la curva de edad (`progresion.ts`: 19-23 de {2,5} a {3,8}; 24-27 de {1,3} a {2,5}; 28-31 de {-1,1} a {-2,3}, ya no es una meseta chata; 32+ de {-5,-2} a {-7,-3}) y los deltas de las decisiones de riesgo (`decisionesRiesgo.ts`: deltas más grandes en las 3 temáticas existentes + una cuarta nueva, "Cirugía preventiva", ±8). Un test (`puede retirarse sin haber pisado nunca la NBA...`) dependía de una coincidencia frágil de la curva vieja (el mínimo determinista pegaba justo debajo del umbral) — se rediseñó para construir el escenario directamente (jugador a los 35 años, edad en la que la curva solo declina) en vez de depender de esa coincidencia.
  - Verificado jugando en el navegador: camino "Vía EEUU" ofrece Villanova/Gonzaga/G League Ignite reales; una carrera cruzó el umbral del Draft en solo 2 temporadas (OVR 44→55); chip de resultado y escudos NBA reales (Utah Jazz) visibles; sin errores de consola. 60 tests Vitest pasando, `tsc -b` y `oxlint` limpios.
  - Pendiente: seguir probando la animación de conteo en vivo por más tiempo (el fix del bug de remontaje está verificado por código y por ausencia de errores, pero no se capturó la animación a mitad de camino en una screenshot — es difícil de capturar con herramientas de screenshot estático).
- **2026-07-27 (cont. 13)**: tres pedidos puntuales del usuario, todos implementados y verificados jugando:
  - **Traspasos siempre con 3 opciones: 2 clubes distintos + "quedarte en tu club actual"** (mismo patrón real de Copero — "Mercado de pases"/"Quedarte en X"). `eventos.ts` ganó `elegirTraspasoOfrecido(equipos, ovrActual, clubActual, azar)` (filtra el club actual del pool de ofertas nuevas, arma 2 + agrega el actual al final). `motorCarrera.ts` usa esta función para los eventos periódicos en NBA (`trade`) y en pre-NBA una vez que ya hay club asignado (`club-liga-domestica`) — el evento inicial (sin club todavío) sigue usando `elegirEquiposOfrecidos` sin opción de quedarte, tiene sentido porque no hay nada en lo que "quedarte" todavía. `PantallaEvento.tsx` marca visualmente la card de "quedarte" distinto (borde hueso en vez de naranja, etiqueta "QUEDARTE ACÁ" en vez del riesgo del club).
  - **Cada elección de equipo sube el OVR de forma azarosa** (verificado jugando Copero: el crecimiento no viene solo de la curva de edad y las decisiones de riesgo, también de elegir club). `src/engine/crecimientoPorTraspaso.ts` (`generarCrecimientoPorTraspaso`, +1 a +4) se aplica en `elegirOpcion` cada vez que se resuelve un evento de club (incluso al elegir "quedarte" — sigue siendo una temporada resuelta). `elegirOpcion` ahora recibe `azar` como tercer parámetro (antes no lo necesitaba); `App.tsx` pasa `Math.random`.
  - **Valor de mercado del jugador** (€), mismo tipo de dato que muestra Copero. `src/engine/valorMercado.ts` (`calcularValorMercadoEuros`: curva `(ovr-40)^4 * 10`, crece rápido en los tramos altos; `formatoValorMercado`: K/M como Copero). Mostrado en el HUD de `PantallaEvento.tsx` y junto al OVR en `PantallaProgreso.tsx`.
  - Un test viejo (`puede retirarse sin haber pisado nunca la NBA...`) se volvió a romper con el nuevo crecimiento-por-traspaso (ya se había ajustado una vez por el ensanche de la curva de edad) — se lo hizo robusto de una vez fijando `potencial: 50` directo en el escenario de test (por debajo del umbral), en vez de depender de que ninguna combinación de mecánicas de crecimiento futuras vuelva a colar al jugador por encima de 55.
  - Verificado jugando en el navegador: evento de traspaso con Peñarol/San Lorenzo/"Ferro Carril Oeste — QUEDARTE ACÁ" (el club real donde ya estaba), VALOR €384K visible en el HUD. Sin errores de consola. 71 tests Vitest pasando, `tsc -b` y `oxlint` limpios.
- **2026-07-27 (cont. 14)**: bug reportado por el usuario — con liga doméstica, el Draft NBA podía aparecer en la primera temporada sin dar opción de quedarse en el club de origen (te "llamaba la NBA" de forma obligatoria). Se corrigió: el evento de Draft ahora reutiliza `elegirTraspasoOfrecido` (2 franquicias NBA + tu club/universidad actual como "QUEDARTE ACÁ", igual patrón que cualquier otro traspaso). `elegirOpcion` en `motorCarrera.ts`: `pasaANba` ahora es `tipo === 'draft' && equipo.id !== clubActual?.id` — si elegís quedarte, seguís en fase `pre-nba` con el mismo club, y como el umbral sigue cruzado, el Draft vuelve a aparecer en la próxima decisión (no se pierde la oportunidad, solo se pospone). 2 tests nuevos (el Draft incluye la opción de quedarte / elegirla declina y mantiene pre-nba). Verificado jugando en el navegador: Draft mostrando Utah Jazz / Charlotte Hornets / "Instituto (Córdoba) — QUEDARTE ACÁ". 73 tests Vitest pasando, `tsc -b` y `oxlint` limpios.
- **2026-07-27 (cont. 5)**: dos pedidos más del usuario sobre el camino pre-NBA. (1) **Elección explícita de camino**: aunque tu nacionalidad tenga liga curada, podés elegir igual "modo universidad" (`crearCarrera(azar, nacionalidad, { modoCaminoPreNba: 'universidad' })`) para probar la vía EEUU sin importar de dónde sos. (2) **Velocidad de decisiones tipo Copero** (Intensa/Normal/Exprés): `src/engine/caminosPreNba.ts` ganó `INTERVALO_TEMPORADAS_POR_DIFICULTAD` (intensa=1, normal=2 default, expres=4 — valores estimados, Copero solo confirma "Normal = cada 2 temporadas" en su UI, el resto es supuesto razonable a ajustar si hace falta). `motorCarrera.ts`: `Carrera.intervaloTemporadas` fijado al crear la carrera; `avanzarSiCorresponde` ahora avanza ese número de temporadas por llamada (acumulando historial de cada una) antes de generar el próximo evento de trade en fase NBA — excepto el Draft, que se dispara apenas se cruza el umbral sin esperar a que termine el bloque, para que elegir Exprés no demore artificialmente el momento de ser drafteado. 33 tests Vitest pasando, `tsc -b` y `oxlint` limpios. Próximo paso: decidir con el usuario si arrancamos la UI (Fase 2) sobre este motor, o seguimos sumando mecánica (renovación de contrato, eventos negativos, más países/escudos reales) antes.
- **2026-07-27 (cont. 15)**: repo publicado en GitHub (`github.com/lautibruno/picknroll`) para deploy en Vercel — commit inicial con toda la app (motor + UI + 67 clubes con escudos + animaciones), listo para importar como proyecto Vite sin configuración extra (sin backend en el MVP, todo client-side).
- **2026-07-27 (cont. 16)**: feedback del usuario probando en el celular real — la pantalla de elegir club/franquicia (y las demás pantallas de evento) requerían scroll en mobile, y la animación del aro quedaba fuera de la vista inicial. Rediseño mobile-first de `PantallaEvento.tsx` sin tocar el layout de desktop (breakpoint `sm:` en adelante queda idéntico a como estaba):
  - Cards de club/franquicia: en mobile pasan de apiladas verticalmente (`grid-cols-1`) a **siempre 3 en fila horizontal** (`grid-cols-3` sin el prefijo `sm:`), con la info recortada a lo esencial (escudo + nombre + botón ELEGIR) — se ocultan barra de nivel de plantel, rol/minutos estimados y la etiqueta de riesgo, todo eso reaparece desde `sm:` en adelante.
  - `AnimacionAro.tsx`: el escenario del aro se dibuja igual que siempre (coordenadas sin tocar) pero ahora vive dentro de un div envoltorio con `scale-[0.4]` en mobile y `sm:scale-100` en desktop — un solo `transform: scale()` sobre todo el contenido, así no hubo que reescribir cada posición a mano. Contenedor exterior pasa de `h-40` fijo a `h-16 sm:h-40`.
  - Hud (marcador superior) y los bloques de decisión de riesgo / especialización recibieron el mismo tratamiento: tipografía y paddings más chicos en mobile, textos secundarios (bajada explicativa, notas de "no hay deshacer", nombre "PICK'N'ROLL" del header) ocultos con `hidden sm:block`/`sm:inline` en mobile para no competir por espacio vertical.
  - Contenedor raíz de las 3 pantallas de evento pasa de altura natural a `h-dvh sm:h-auto` (ocupa exactamente el alto visible del viewport en mobile, vuelve a altura natural en desktop).
  - Verificado a mano en el navegador con viewport mobile real (375×812): pantalla de cantera con San Lorenzo/Estudiantes/Boca Juniors (escudos reales) visible completa sin scroll incluida la animación del aro arriba; Draft NBA con Utah Jazz/Brooklyn Nets también entera; decisión de riesgo "Cirugía preventiva" entera. Se jugó una elección real de principio a fin (tap → animación → confirmación → HUD actualizado) para confirmar que el flujo sigue funcionando, no solo el layout estático. Desktop (966px) verificado sin regresiones — mismo diseño rico de siempre. 86 tests Vitest, `tsc -b` y `oxlint` limpios (cambio 100% de presentación, sin tocar el motor).
- **2026-07-27 (cont. 17)**: feedback duro e inmediato del usuario con captura — la simplificación anterior quedó "mega feo, chiquito, sin onda y sin una misera información" en mobile real. Se cargó el skill `ui-ux-pro-max` (el CLI no está instalado en esta máquina — mismo symlink roto documentado en otras entradas — se aplicaron sus reglas de memoria/checklist directamente). Diagnóstico: la vuelta anterior ocultó demasiada información real (nivel de plantel, rol/minutos, etiqueta de riesgo) cuando en realidad, una vez compactada la animación del aro, sobraba mucho espacio vertical sin usar en mobile — la solución correcta no era esconder contenido sino agrandarlo.
  - Cards de club/franquicia en mobile: escudo más grande (56px vs 36px), se **restauran** la barra de nivel de plantel, el bloque ROL/MIN·PJ y la etiqueta de riesgo (antes ocultos con `hidden sm:block`), todo en versión compacta con tipografía más chica en vez de invisible. Feedback táctil nuevo (`active:scale-[0.98]`) en el tap de la card, siguiendo la regla de `ui-ux-pro-max` de dar respuesta visual al toque.
  - `AnimacionAro.tsx`: escala de mobile subida de 0.4 a 0.5 (contenedor `h-16`→`h-20`) — más presencia sin volver a comerse toda la pantalla.
  - **Bug real encontrado y corregido en la propia vuelta anterior**: el contenedor de las pantallas de evento usaba `h-dvh` a secas, pero vive anidado dentro del `<main>` de `App.tsx` que tiene `py-10` (80px de padding vertical fijo) — `h-dvh` mide el viewport completo sin descontar ese padding externo, así que el documento terminaba siendo 80px más alto que la pantalla real y sí hacía falta scroll aunque visualmente pareciera que sobraba espacio. Se corrigió a `h-[calc(100dvh-5rem)]` (5rem = los 80px de `py-10`) en las 3 pantallas de evento. Confirmado con `document.documentElement.scrollHeight === window.innerHeight` exacto (812px = 812px) en vez de scrollear "por poco".
  - **Segundo bug propio, encontrado en la verificación de desktop**: el `line-clamp-1` agregado a la etiqueta de riesgo para evitar que se desborde en mobile se aplicaba también en `sm:` y arriba, truncando "RIESGO MEDIO · ROTACIÓN" a "RIESGO MEDIO ·_" en pantallas grandes donde antes se veía completo. Se agregó `sm:line-clamp-none` para que el corte de línea sea solo-mobile.
- **2026-07-27 (cont. 18)**: **playoffs reales + mensajería de temporada** + rebalanceo de la curva de OVR, tres pedidos del usuario en un mismo mensaje ("implementar para jugar playoffs... mensajería de cómo nos fue en cada temporada... jugada final al azar... el cálculo de OVR anda medio flojo... debe ser más aislado").
  - `src/engine/playoffs.ts` (nuevo): solo aplica en fase NBA. `nivelEquipoCombinado` mezcla nivel de club + OVR del jugador en partes iguales. `simularTemporadaRegular` corre 82 partidos con una probabilidad de victoria derivada de ese nivel combinado y clasifica a playoffs si el récord supera un umbral (~.500). Si clasifica, `simularPlayoffs` corre 3 rondas (Primera ronda/Semifinal/Final, nombres reales de básquet) contra un **rival ficticio** (`RIVALES_FICTICIOS` — nombres inventados a propósito, no franquicias NBA reales, para no generar la falsa impresión de que hay roster real del otro lado) en series al mejor de 3.
  - **Jugada final** (pedido explícito: "cuando tenemos buen nivel, en playoff, lista de decisiones al azar... finta y penetrar / tirar el triple"): implementada como evento interactivo nuevo `jugada-final` en `motorCarrera.ts`, pero **deliberadamente aislada** (pedido explícito: "hay muchas decisiones que tomar... debe ser más aislado") — solo puede aparecer en la Final (ronda 3), solo si el jugador tiene OVR≥78, solo si la serie llegó al partido decisivo (1-1), y ahí solo 1 de cada ~3 veces. La inmensa mayoría de las temporadas con playoffs se resuelven solas, sin pedirle nada al jugador — la decisión de diseño fue no tocar la cadencia general de trade/riesgo (que ya tiene sus propios tests) sino hacer que esta pieza nueva específica sea rara por construcción (grosor NBA≥78 y encima el string de round-3-empatada-1-1 y encima 35% de probabilidad, todo a la vez).
  - `motorCarrera.ts`: `Carrera` ganó `resumenTemporada` (récord + resultado de playoffs de la última temporada NBA) y `estadoPlayoffsPendiente` (contexto para retomar la simulación cuando se resuelve la jugada final). Nuevo tipo de evento `jugada-final`. Los **anillos ya no salen de una probabilidad suelta** (como antes en `trofeos.ts`) — ahora se ganan de verdad ganando las 3 rondas simuladas; `evaluarTrofeosTemporada` se simplificó, ya no toca `anillos` (solo All-Star/MVP, que siguen igual).
  - **Curva de OVR rebalanceada** (pedido explícito: "el cálculo de GRL/OVR anda medio flojo") — diagnóstico: el 70% de las carreras tenían potencial techo 60-75 arrancando ya en 40-50, así que se estancaban a los 3-4 años y quedaban 15+ temporadas sin nada que crecer. Se corrieron los tramos de `potencial.ts` unos puntos arriba (50%/30%/13%/7% en 62-78/79-87/88-93/94-99, antes 70%/20%/8%/2% en 60-75/76-85/86-92/93-99) y se rehizo `progresion.ts`: el crecimiento ahora se reparte en más temporadas (19-26 en vez de solo 19-23), con un pico real 27-29 que todavía puede subir un poco (no una meseta chata como antes) y el declive fuerte recién arranca en serio a los 34-36 en vez de sentirse como un techo a los 28.
  - `PantallaProgreso.tsx` muestra el nuevo resumen como chip (mismo patrón visual que el chip de decisión de riesgo ya existente): "TEMPORADA REGULAR · V-D" + "🏆 Campeones de playoffs" / "Eliminados en {ronda} vs {rival} ({marcador})" / "No clasificaron a playoffs" según corresponda. `PantallaEvento.tsx` ganó una pantalla nueva para la jugada final (mismo patrón de 2 cards que riesgo/especialización).
  - Verificado jugando una carrera real de punta a punta en el navegador: la curva de OVR ahora sube de forma sostenida (44→66 en 14 temporadas, con subidas visibles en varios tramos en vez de estancarse a los 3 años), el chip de temporada regular aparece con el récord real, y se llegó a jugar playoffs de verdad — "TEMPORADA REGULAR · 46-36" + "ELIMINADOS EN PRIMERA RONDA VS UNION BLAZE (0-2)" mostrado correctamente. La jugada final en sí (evento interactivo) se verificó con tests dedicados en `playoffs.test.ts` y `motorCarrera.test.ts` (construyendo el estado intermedio directamente, dado lo aislado/raro que es por diseño llegar a esa combinación exacta de condiciones con RNG real) — no se pudo forzar su aparición en esta sesión de navegador sin manipular el estado interno de React. 107 tests Vitest (17 nuevos: `playoffs.test.ts` completo + integración en `motorCarrera.test.ts`), `tsc -b` y `oxlint` limpios.
  - Verificado a mano: mobile (375×812) con escudo/nivel/rol/riesgo visibles y sin scroll (`scrollHeight` = `innerHeight` exacto) en cantera y Draft NBA; desktop (966px) sin la regresión del texto truncado. 86 tests Vitest, `tsc -b` y `oxlint` limpios.
- **2026-07-28**: pedido de calcar Copero de verdad — jugué copero.com.ar en vivo para diagnosticar. Cambios: (1) crecimiento de OVR ahora sale casi todo de fichar/préstamo, escalado por qué tan por encima está el club nuevo de tu nivel (`crecimientoPorTraspaso.ts` rehecho), curva de edad pasiva eliminada salvo declive de veterano (`progresion.ts`). (2) decisión de riesgo ya no da +N/-N directo — ahora es "Competir" vs "Aceptar rotación", cambia tu ROL (titular/rotación) que modula el crecimiento del próximo fichaje (`decisionesRiesgo.ts`). (3) `PantallaProgreso`+`PantallaEvento` fusionadas en `PantallaCarrera.tsx`: dashboard único y continuo (header/vitrina/historial siempre visibles), sin pantalla intermedia "seguir jugando" — `elegirOpcion` ahora encadena directo a la próxima decisión.
  - Bug real encontrado y corregido jugando: al fusionar en un componente persistente, `eligiendoId` (estado de la animación de fichaje) dejó de resetearse entre decisiones porque el componente ya no se remonta — quedaba trabado en el club anterior. Se agregó un `useEffect` que lo resetea cuando cambia el evento pendiente.
  - Verificado jugando en el navegador: club grande cruza el Draft en una sola elección, "Competir"→"Ganaste la titularidad" sin delta numérico, especialización sube 3PM visiblemente, sin pantallas intermedias ni el bug de fichado trabado. 112 tests, `tsc -b` y `oxlint` limpios.
- **2026-07-28 (cont.)**: feedback duro tras la fusión — mobile roto (perdió el grid horizontal de 3 clubes), demasiadas decisiones de rol, y un bug real: el chip verde "+N" de OVR se calculaba contra `historial.at(-2)` en vez de contra el OVR justo antes de la decisión resuelta, así que mostraba números que nunca coincidían con lo que pasaba.
  - Fix de fondo: `Carrera` ganó `ovrAlIniciarDecision`/`ultimoCambioOvr` — se ancla el OVR al generar cada decisión y se compara contra eso, no contra el historial. Probabilidad de "competencia por el puesto" bajada de 50% a ~20% (mucho más ofertas de club simples). Grid de clubes vuelve a ser horizontal (3 columnas) y compacto en mobile, con `.animar-ovr` (pop real) en el número grande.
  - Verificado jugando en el navegador: +2 en el chip coincide exacto con 50→52 en el historial; "Competir" ganado no muestra ningún número falso; mobile (375×812) con el grid de 3 clubes legible. 114 tests, `tsc -b` y `oxlint` limpios.
- **2026-07-28 (cont. 2)**: captura de Copero pidiendo esa densidad exacta + 4 pedidos más: crecer seguía siendo casi imposible, decisiones se duplicaban a veces, faltaba un revelado en vivo del azar (tipo "ta-te-ti"), y sacar la animación de aro de los fichajes normales.
  - Bug real encontrado: `AnimacionAro` + el reset de `eligiendoId` por `useEffect` competían en una carrera de estados — a veces el vuelo de la pelota se re-disparaba con el club viejo justo cuando ya había una decisión nueva, duplicando la resolución. Se sacó la animación de los fichajes (resuelven al toque) y se borró `AnimacionAro.tsx` (ya no se usa en ningún lado).
  - `crecimientoPorTraspaso.ts` reescrito: el desafío del club ya nunca resta (piso en 0, antes bajaba a -6), y el margen de nivel de las ofertas subió de 10 a 18 — antes al acercarte al techo del pool ya no aparecían clubes desafiantes y quedabas estancado en negativo.
  - Nuevo: revelado en vivo tipo "ta-te-ti" para "Competir" (alterna Titular/Rotación cada vez más lento y asienta en el resultado real, ya resuelto de antes) — y lo mismo para la jugada final de playoffs, para lo cual `playoffs.ts` ahora precalcula los dos resultados posibles (`resultadoSiFinta`/`resultadoSiTriple`) al generar el evento, para que el revelado sea fiel a lo que en verdad va a pasar.
  - `PantallaCarrera.tsx` reescrito a densidad Copero: header de una sola franja (OVR en badge + club + valor), chips de una línea en vez de bloques, historial sin subtítulos de relleno.
  - Verificado jugando en el navegador: fichajes resuelven al toque sin duplicar, crecimiento sostenido (48→60 en 4 decisiones, cruzó el Draft), revelado de "Competir" visible y coherente con el resultado, layout denso en mobile y desktop. 114 tests, `tsc -b` y `oxlint` limpios.
- **2026-07-28 (cont. 3)**: pedido de cartel + íconos reales para campeón/All-Star/MVP/Mundial/JJOO. Se agregaron `mundial`/`jjoo` como convocatorias nuevas (ciclo de 4 temporadas, desfasadas entre sí) en `trofeos.ts`, `IconoTrofeo` en `motorCarrera.ts` (marca qué se ganó en cada fila del historial, incluido el anillo marcado retroactivo cuando se gana la Final), y `src/ui/iconosTrofeos.tsx` con 5 SVG propios (no logos oficiales, mismo criterio de riesgo que el resto del proyecto). `PantallaCarrera.tsx` muestra un cartel centrado ~2.8s cuando se gana algo nuevo, más los íconos permanentes en el header y en la fila del historial correspondiente.
  - Verificado jugando en el navegador (con el umbral de All-Star bajado a mano temporalmente para forzarlo rápido, después revertido): el header acumula ★ correctamente (2→3→5) y cada fila del historial marca la estrella en la temporada exacta en que se ganó. El cartel en sí no se pudo capturar en una screenshot por la ventana corta de 2.8s vs. la latencia de la herramienta de navegador, pero corre el mismo código que ya se confirmó visualmente (conteos/íconos). 117 tests, `tsc -b` limpio, 3 warnings de lint no bloqueantes (fast-refresh en el archivo de íconos, exhaustive-deps intencional).
- **2026-07-28 (cont. 4)**: pedido explícito de reemplazar por íconos REALES (no inventados). Campeón/Mundial/JJOO ahora son `<img>` a SVG oficiales de dominio público/CC en Wikimedia Commons (trofeo Larry O'Brien, logo FIBA World Cup, aros olímpicos). MVP/All-Star quedan con el dibujo propio: no existe en Commons un trofeo/logo libre y estable para ellos (el trofeo Michael Jordan y los logos de All-Star Game son artwork comercial que cambia cada año). Verificado: las 3 URLs cargan bien (200, image/svg+xml) desde el navegador. 117 tests, `tsc -b` limpio.
- **2026-07-28 (cont. 5)**: 3 pedidos más. (1) Rival de playoffs ahora es franquicia NBA real (`elegirRival` toma `equiposNba`/`clubActualId` de `motorCarrera.ts`, nunca repite tu club ni un rival ya enfrentado en la misma tanda). (2) La "jugada final" ahora tiene una escena narrativa al azar (`ESCENAS_JUGADA_FINAL` en `playoffs.ts`, mismo patrón que `decisionesRiesgo.ts`) y, junto con la decisión de riesgo normal, dejó de mostrar el % de éxito en la UI (`PantallaCarrera.tsx`) — la mecánica interna no cambió, solo se oculta el dato. (3) Mundial/JJOO recalculados con cadencia real: antes estaban separados 2 y 2 temporadas dentro del ciclo de 4; en la realidad el Mundial cae un año antes que los JJOO siguientes (no equidistantes) — `trofeos.ts` ahora usa `edad % 4 === 0` para Mundial y `edad % 4 === 1` para JJOO (la temporada inmediata siguiente).
  - Verificado jugando una carrera completa en el navegador: agencia libre mostró siempre equipos NBA reales (Raptors, Hornets, Mavericks, 76ers, Heat, Pacers), los paneles de riesgo mostraron "ir por la titularidad"/"garantizado" sin porcentaje, y se ganó un anillo real (ícono real en el header). No se disparó Mundial/JJOO/Jugada Final por azar en esa corrida puntual, pero la lógica de cadencia y escenas está cubierta por tests unitarios. 119 tests, `tsc -b` limpio, mismos 3 warnings de lint preexistentes no bloqueantes.
- **2026-07-28 (cont. 6)**: pedido de animación distinta si el OVR sube o baja (antes había un solo "pop" genérico ya existente en `useNumeroAnimado.ts`/`OvrAnimado`, sin diferenciar dirección). Se agregó `--color-baja` (rojo) en `index.css` junto a `--color-en-vivo` (verde), y dos pares de keyframes: `ovr-glow-sube`/`ovr-glow-baja` (destello de color sobre el badge de OVR, además del pop que ya existía) y `delta-pop`/`delta-pop-baja` (el "+N"/"-N" flotante ahora deriva hacia arriba en verde si sube, hacia abajo en rojo si baja — antes el delta negativo se veía gris, sin distinguirse de "sin cambio"). `OvrAnimado` en `PantallaCarrera.tsx` elige la clase según `objetivo` vs `inicial`.
  - Verificado en el navegador: al fichar a un club mejor, la clase `animar-ovr-sube` se aplica al badge de OVR (confirmado leyendo el DOM, ya que la animación de 0.9s es muy corta para capturarla en una screenshot por la latencia de la herramienta) y el delta mostró "+1" en verde con `animar-delta`. No se pudo forzar naturalmente una baja de OVR en esa sesión de prueba (el fichaje siempre sumó, aunque sea poco) — la rama `animar-ovr-baja`/`animar-delta-baja` es simétrica en el mismo componente y ya pasa `tsc -b`, queda pendiente de una confirmación visual futura si se da el caso en una partida real. 119 tests, `tsc -b` limpio.
- **2026-07-28 (cont. 7)**: usuario compartió un mockup de diseño (zip "PickNRoll sistema de diseño con vitrina") con una vitrina 3D de trofeos para la pantalla de retiro, pidiendo explícitamente que los trofeos sean "EXACTAMENTE los reales" (copa NBA, oro olímpico = medalla, etc.), agrupados por tipo, con descripción al pasar el mouse o tocar. El mockup es solo referencia visual (shapes CSS dibujados a mano, sin datos reales) — no se copió tal cual: se construyó la vitrina en `PantallaRetiro.tsx` con los 5 tipos de trofeo reales que la carrera efectivamente trackea (anillo/MVP/All-Star/Mundial/JJOO), reusando `ICONOS_TROFEO` de `iconosTrofeos.tsx` (ya la agrupación es 1 tile por tipo con su cantidad, que es como está modelado `Trofeos` — no hace falta lógica extra de agrupamiento). Se agregó `DESCRIPCION_TROFEO` (un texto por tipo) y un tap/click en cada tile despliega esa descripción abajo (mobile-friendly, más un `title` nativo para hover en desktop).
  - Se subió JJOO de "aros olímpicos" (el logo del evento) a una **medalla de oro real** (`Gold_medal_olympic.svg`, CC BY-SA 2.5, Wikimedia Commons) — más fiel a "medalla olímpica" que pidió el usuario, ya que los aros son el logo, no el premio en sí.
  - Se buscó de nuevo (más a fondo, con `site:commons.wikimedia.org`) un trofeo/logo libre para MVP (trofeo Michael Jordan) y All-Star — se confirmó otra vez que no existe ninguno con licencia libre en Wikimedia Commons (son artwork comercial de la NBA, sin versión de dominio público). Quedan con el ícono dibujado propio, igual que la vez anterior.
  - No se agregó una categoría de "copas locales"/"copas continentales" (aparecían en el mockup de referencia) porque el motor del juego no trackea títulos de liga doméstica como estadística — agregarla sería una feature nueva no pedida explícitamente; queda anotado como posible pedido futuro si el usuario lo confirma.
  - Verificado jugando una carrera completa hasta el retiro: la vitrina muestra los 5 tipos con sus íconos reales, el trofeo de campeón (Larry O'Brien) se ve resaltado con cantidad 1, y al tocarlo aparece la descripción "Campeón de la NBA — Trofeo Larry O'Brien, ganado en los playoffs." 119 tests, `tsc -b` limpio.
- **2026-07-29**: 7 pedidos del usuario tras probar (incluye un bug real y una crítica válida a la vitrina anterior).
  - **(5) Bug real arreglado**: errar el tiro decisivo igual mostraba el título. Causa: al resolver la jugada final, `avanzarSiCorresponde` seguía simulando la temporada SIGUIENTE en el mismo llamado y sobreescribía `resumenTemporada` — si esa salía campeona, el campeonato aparecía pegado al tiro errado. Se agregó `Desenlace`/`desenlacePendiente` a `Carrera` + `continuarCarrera()`: todo título (anillo, Mundial, JJOO, liga local) frena la carrera con su propio cartel y botón CONTINUAR antes de seguir. Test explícito del caso.
  - **(3)** Arriesgar mueve el OVR: `cambioOvrPorRiesgo` en `decisionesRiesgo.ts` (+4 al ganar, -2 al perder — asimétrico a propósito para que competir siga siendo atractivo). Va además del efecto de rol que ya existía.
  - **(7)** Mundial/JJOO dejaron de otorgarse por azar puro: nuevo `convocatorias.ts` + evento `convocatoria` con dos opciones; `convocatoriaDisponible()` en `trofeos.ts` solo decide si te convocan (calendario + OVR), ganarlo depende de la decisión.
  - **(4)** Título de liga local en fase pre-nba (`probabilidadTituloLocal`: arranca en 6%, sube 5% por temporada consecutiva en el mismo club, techo 40%). `trofeos.ligaLocal` + `temporadasEnClubActual` + `Carrera.ligaDomestica`. **Trofeos reales**: se buscó foto del trofeo físico para las 10 ligas curadas — solo existe con licencia usable para **ACB (España)** y **NBL (Australia)**; las otras 8 usan el **logo oficial real** de la liga (decisión confirmada con el usuario: "trofeo real donde exista + logo si no"). Campo `trofeoEsCopaReal` marca cuál es cuál. Las 11 URLs verificadas con fetch 200 en el navegador.
  - **(1)** Vitrina rehecha de cero — la anterior eran tiles planos, no se parecía al mockup. Nuevo `VitrinaTrofeos.tsx`: mueble de madera con perspectiva, dos estantes, reflejo de vidrio y divisor central (igual que el mockup de Claude Design), pero con las **imágenes reales** de cada trofeo paradas en los estantes en vez de las formas CSS del mockup. Agrupados por tipo con "+N" cuando hay muchos, y tocar/hover muestra la descripción. Se agregó un foco de luz radial detrás de cada pieza: sin eso los logos oscuros (ej. LNB) desaparecían contra el fondo negro del mueble — encontrado verificando.
  - **(2)** Recorrido en la pantalla de retiro: `engine/recorrido.ts` (`resumirRecorrido`, función pura con 6 tests) agrupa temporadas consecutivas por club; la UI lo muestra como línea de tiempo con años, escudo, temporadas y los títulos ganados en ese club.
  - **(6)** Ritmo: `useNumeroAnimado` pasó de 700ms a 950ms y `PantallaCarrera` esconde las opciones nuevas por `DEMORA_OPCIONES_MS` (1100ms) mostrando "JUGANDO LA TEMPORADA…" mientras el OVR termina de animar.
  - Verificado jugando dos carreras completas: título local con el nombre real de la liga ("Saliste campeón de la Liga Nacional de Básquet (Argentina) con Obras Sanitarias"), convocatoria del Mundial como decisión, pausa de "jugando la temporada" antes de cada set de opciones, vitrina con 24 títulos agrupados (×6/×14/×2/×2 con "+N") y recorrido de 7 equipos. El caso puntual de errar la jugada final no se pudo forzar en vivo (es raro por diseño) pero tiene test propio. 143 tests, `tsc -b` limpio, 4 warnings de lint preexistentes no bloqueantes.
- **2026-07-29 (cont.)**: tres bugs reportados jugando + dos ajustes de UI.
  - **Cartel de All-Star trababa el juego** ("me quedo trabado en all star"): el `setTimeout` que cerraba el cartel se armaba dentro del mismo `useEffect` cuya dependencia era `historial.length`. Si el historial volvía a crecer antes de los 2.8s (pasa cuando en la misma temporada ganás All-Star Y un título, y tocás CONTINUAR), el cleanup mataba el timer sin re-armarlo y el overlay quedaba para siempre bloqueando todos los clics. Ahora el timer vive en su propio efecto atado a `cartelTrofeos`, y además el cartel es `pointer-events-none` (es decorativo, no debe poder atrapar clics nunca) como segunda red.
  - **"Gané 3 títulos seguidos sin jugar"**: era un problema de diseño de lo que había hecho antes — el título CORTABA el bloque de temporadas, así que cada CONTINUAR arrancaba una temporada nueva y un equipo dominante encadenaba campeonatos sin que apareciera ni una decisión. Se reemplazó `desenlacePendiente: Desenlace|null` por `desenlacesPendientes: Desenlace[]` (cola): el bloque se juega completo, los títulos se encolan, y la próxima decisión ya viene resuelta DETRÁS de los carteles. `continuarCarrera(carrera)` ahora solo saca un cartel de la cola (no simula nada, y no necesita `azar`/`equiposNba`). Test de regresión explícito: tras un título, `eventoPendiente` no es null y descartar el cartel no agrega historial ni trofeos.
  - **Convocatoria pre-emptaba los playoffs**: el chequeo de Mundial/JJOO estaba ANTES de simular temporada+playoffs, así que en año de Mundial no se jugaban los playoffs de esa temporada. Se movió después (la selección juega en el receso, como en la realidad).
  - **UI**: se sacó el nivel del equipo (número + barra) y el pronóstico de crecimiento ("CRECÉS BIEN") de las tarjetas de club — pedido explícito: "que sea algo más genuino por decisión del que está jugando, que no se guíe por potencial". El motor sigue usando `nivel` por dentro para todo el balance, solo deja de mostrarse; se conservó el rol (titular/rotación/banca) porque es algo que un jugador sí sabe al firmar. Y las tarjetas se agrandaron (escudo 48→64px, nombre a `text-sm`, más padding) manteniendo 3 columnas y la estética, verificado en desktop y mobile 375px.
  - Verificado en el navegador: cartel de campeón con el trofeo real de LNB y la decisión siguiente ya visible detrás, flujo de decisión-tras-decisión sin cadenas de CONTINUAR, tarjetas nuevas en desktop y mobile. 144 tests, `tsc -b` limpio, mismos 4 warnings de lint preexistentes.
- **2026-07-30**: logo real de All-Star, cancha, musculosa y vitrina superpuesta.
  - **All-Star**: el logo permanente (estrella + logoman) **no existe en Wikimedia ni en Wikipedia** — solo están los de cada edición/ciudad (2018, 2019, 2022, 2023, 2025, Washington 2001). Se buscó cuatro veces antes de frenar. Lo aportó el usuario y es el ÚNICO trofeo servido desde `public/trofeos/all-star.png` en vez de Wikimedia. Lección anotada en memoria: si un asset real no aparece a la segunda búsqueda, pedírselo en vez de seguir buscando o sustituirlo.
  - **Cancha** (`PasoPosicion.tsx`): la línea de triple estaba dibujada en el extremo OPUESTO al aro (bug reportado). Ahora rodea el aro de abajo: rectas de esquina desde la línea de fondo + arco de radio 126 centrado en el aro, verificado midiendo `getBBox()` de los paths y no a ojo. Se sumaron tablero, aro, semicírculo de zona restringida y marcas de rebote. Piso rehecho como parquet de maple claro: tono base cálido + variación tabla por tabla + veta fina + juntas + brillo de barniz + viñeta. Los marcadores de PF/C se corrieron para no tapar el aro.
  - **Musculosa** (`PasoCamiseta.tsx`): era una remera con mangas; ahora es una musculosa real — sisas profundas, escote redondo, ribetes en color secundario, paneles laterales, tela de malla (`<pattern>`), pliegues sombreados, sombra proyectada y dorsal con contorno. La geometría vive en constantes (`CONTORNO_MUSCULOSA`, `ESCOTE`, `SISA_*`, `RUEDO`) para que el contorno y las costuras siempre coincidan.
  - **Vitrina**: ya no se corta en 3 piezas con "+N" — se muestran TODAS, superpuestas, y el solapamiento se adapta a la cantidad (`solapeDe`: 2 separadas, 3 tocándose, y de ahí en más se van montando hasta un tope de 34px). Verificado con 17 títulos: 1 anillo suelto, 4 MVP apenas montados, 12 All-Star bien agrupados.
  - De paso, los íconos de trofeo pasaron de caja cuadrada a alto fijo con ancho libre: cada trofeo real tiene su proporción (la copa O'Brien es alta y angosta, el logo del All-Star es ancho) y forzarlos a un cuadrado dejaba a los anchos diminutos.
  - 144 tests, `tsc -b` limpio.
- **2026-07-30 (cont.)**: vitrina superpuesta y 10 decisiones distintas.
  - **Vitrina**: dejó de cortar en 3 piezas con "+N". Ahora se muestran TODAS superpuestas y el solape se adapta a la cantidad (`solapeDe` en `VitrinaTrofeos.tsx`: 2 separadas, 3 tocándose, y de ahí en más se van montando hasta un tope de 34px). Verificado con 17 títulos: 1 anillo suelto, 4 MVP apenas montados, 12 All-Star bien agrupados.
  - **Decisiones**: `decisionesRiesgo.ts` reescrito — de 4 temas que siempre ofrecían "Competir/Aceptar rotación" a **10 situaciones distintas**, cada una con sus propias etiquetas de botón, textos de revelado y resultado. Lista propuesta al usuario y aprobada antes de implementar (regla del CLAUDE.md).
    - Tres niveles de impacto en el OVR, relación 2:1 en los tres (valores confirmados por el usuario): **bajo +2/−1** (5 decisiones: doble turno, 300 tiros, liga de verano, cambio de posición, entrenar con el veterano), **medio +4/−2** (3: competencia por el puesto, nuevo cuerpo técnico, vuelta de lesión), **alto +8/−4** (2: operarte o aguantar, cambiar de representante).
    - Selección con peso por impacto (bajo 3 / medio 2 / alto 1): las de impacto alto son raras pero no tanto como para no verlas nunca en una carrera.
    - **Solo las de impacto medio mueven el rol** titular/rotación — doblar turno en el gimnasio no debería sacarte del equipo. `ResultadoRiesgo.rol` pasó a `Rol | null` y el chip de la UI solo muestra TITULAR/ROTACIÓN cuando corresponde.
    - Frecuencia bajada de 0.22/0.20 a **0.15** en ambas fases (pedido: "que no sea tan seguido").
  - Verificado en el navegador subiendo la frecuencia a mano: salieron 4 decisiones distintas seguidas con sus botones propios (Liga de verano → JUGAR EL VERANO/DESCANSAR, Entrenar con el veterano → IR CON ÉL/ENTRENAR POR TU CUENTA, Doble turno, 300 tiros por día); después se revirtió a 0.15. 153 tests, `tsc -b` limpio.
